const gender = 'M';
gender === 'M' ? console.log("You are a man"): console.log("You are a woman");

let age = 20;
age >= 18 ? console.log("You are an adult") : console.log("You are a child");

age = 18;
const canVote = age >= 18 ? 'You can vote' : 'You cannot vote';
console.log(canVote);

//Serves as a one line replacement for an if-else statement
//It's syntax is (condition) ? ifTrue : ifFalse;
